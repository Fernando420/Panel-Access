<div class="row">
    <table class="table table-striped">
        <thead>
            <th>Name</th>
            <th>Last Name</th>
            <th>Member Id</th>
            <th>Email</th>
        </thead>
        <tbody>
            <% @clients.each do |client| %>
                <tr>
                    <td><%= client['name'] %></td>
                    <td><%= client['last_name'] %></td>
                    <td><%= client['member_id'] %></td>
                    <td><%= client['email'] %></td>
                </tr>
            <%end%>
        </tbody>
    </table>
</div>