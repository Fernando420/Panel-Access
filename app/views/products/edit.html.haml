%div.row
  %div.card
    %h1= t('products.edit_subtitle')
    = form_for :product, :url => { :action => "update"}, method: 'PUT', :html => {:class => "form row"} do |f|
      = render partial: 'form_fields', locals: {f: f}